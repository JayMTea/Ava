var e={spend:`Cloud API dollars spent in this window. Running Ava on your own hardware is free — this only moves when it calls a paid cloud model.`,energy:`Estimated electricity the GPU used, in kilowatt-hours. Recent hours use your GPU's own power readings where it reports them; older data and non-reporting GPUs use a nominal wattage, so a long window is mostly an estimate.`,throughput:`Average generation speed in tokens per second, across all models. Higher means Ava writes replies faster.`,ttft:`Time to first token — how long after you send a message before Ava starts replying, in seconds. Lower feels snappier.`,routeErrors:`How many times the model router had to fall back to a backup model because the primary failed. Zero is ideal.`,gpuUtil:`How busy the GPU’s compute cores are right now, 0–100%. High while Ava is answering, near zero when idle.`,gpuTemp:`GPU core temperature in °C. Sustained high temperatures can make the card throttle and slow down.`,memory:`Share of GPU/system memory in use, 0–100%. Close to 100% risks out-of-memory errors during large jobs.`,cpu:`Overall CPU utilisation across all cores, 0–100%.`,gpuPower:`Instantaneous GPU power draw in watts — the live number behind the energy estimate.`,throughputSeries:`Tokens per second over time, one line per model. Use the range buttons to zoom from a single day out to five years.`,hardwareSeries:`GPU utilisation, temperature, memory and CPU over time. The range buttons control how far back the chart reaches — limited by your data-retention setting.`,modelShare:`Share of completions handled by each model — shows which brain is doing the work.`,activeTurns:`Chat turns Ava is processing right now — a turn is one request/response exchange.`,recorded24h:`Everything logged to performance history in the last 24 hours — Ava's own completions plus every connected app's work.`,servicesUp:`Backend services (model engine, connectors, schedulers) that are up, out of the total Ava monitors.`,alerts:`Active alerts — budget, hardware, or service issues Ava has flagged. Red means at least one is critical.`};export{e as t};