# generated by agent/docs/arch.py — DO NOT EDIT (edit architecture.yaml)

direction: down

vars: {
  d2-config: {
    layout-engine: elk
  }
}

style: {
  fill: "#ffffff"
}

net: "Untrusted — Internet / LAN" {
  style.stroke: "#94a3b8"
  style.fill: "#ffffff"
  style.font-color: "#c96442"
  style.bold: true
  client: "Phone / Browser" {style.fill: "#f8fafc"; style.font-color: "#1e293b"; style.stroke: "#e2e8f0"; style.stroke-width: 1; style.border-radius: 6; style.font-size: 11}
  webinet: "Open Internet\n(Ava's web reads, via Tor)" {style.fill: "#f8fafc"; style.font-color: "#1e293b"; style.stroke: "#e2e8f0"; style.stroke-width: 1; style.border-radius: 6; style.font-size: 11}
}

perim: "Perimeter — Tailscale TLS + auth gate" {
  style.stroke: "#94a3b8"
  style.fill: "#ffffff"
  style.font-color: "#c96442"
  style.bold: true
  authgate: "Auth gate (phone_bridge)\nHMAC-signed cookie · 30d\nper-IP login throttle" {style.fill: "#f8fafc"; style.font-color: "#1e293b"; style.stroke: "#e2e8f0"; style.stroke-width: 1; style.border-radius: 6; style.font-size: 11}
}

host: "Host — loopback 127.0.0.1 only" {
  style.stroke: "#94a3b8"
  style.fill: "#ffffff"
  style.font-color: "#c96442"
  style.bold: true
  bridge: "phone_bridge :8096\n/api (session-gated) · /internal (token-gated)" {style.fill: "#f8fafc"; style.font-color: "#1e293b"; style.stroke: "#e2e8f0"; style.stroke-width: 1; style.border-radius: 6; style.font-size: 11}
  vault: "Secrets — chmod 0600\ndata/auth_password · data/.secret\n~/.config/tstruct/auth.json" {shape: page; style.fill: "#f8fafc"; style.font-color: "#1e293b"; style.stroke: "#e2e8f0"; style.stroke-width: 1; style.border-radius: 6; style.font-size: 11}
  webout: "Web egress — Tor SOCKS5\nSSRF-guarded · public-IP-only\nfail-closed (no clearnet leak)" {style.fill: "#f8fafc"; style.font-color: "#1e293b"; style.stroke: "#e2e8f0"; style.stroke-width: 1; style.border-radius: 6; style.font-size: 11}
}

sbx: "Sandbox — Docker (no ambient egress)" {
  style.stroke: "#94a3b8"
  style.fill: "#ffffff"
  style.font-color: "#c96442"
  style.bold: true
  agent: "Ava agent 'main'" {shape: hexagon; style.fill: "#f8fafc"; style.font-color: "#1e293b"; style.stroke: "#e2e8f0"; style.stroke-width: 1; style.border-radius: 6; style.font-size: 11}
  guard: "SSRF guard + egress proxy\nper-tool allow-list (deny by default)" {style.fill: "#f8fafc"; style.font-color: "#1e293b"; style.stroke: "#e2e8f0"; style.stroke-width: 1; style.border-radius: 6; style.font-size: 11}
}

net.client -> perim.authgate: "HTTPS / TLS" {style.stroke: "#94a3b8"}
perim.authgate -> host.bridge: "valid session only" {style.stroke: "#94a3b8"}
sbx.agent -> sbx.guard: "every tool call filtered" {style.stroke: "#94a3b8"}
sbx.guard -> host.bridge: "enumerated /internal routes + scoped token" {style.stroke: "#dc2626"; style.stroke-dash: 3}
host.bridge -> host.vault: "reads (never logged)" {style.stroke: "#94a3b8"; style.stroke-dash: 3}
host.webout -> net.webinet: "Tor exit IP — real IP hidden" {style.stroke: "#94a3b8"; style.stroke-dash: 3}
